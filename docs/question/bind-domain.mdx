# 如何用域名访问？

首先将域名 `A` 记录解析到服务器 IP, 这是即可通过 `域名:端口` 访问，如果不想带端口，可使用 `nginx` 或 `caddy` 等工具反向代理. 以下以宝塔面板为例:

首先点击 `网站` -> `新增站点`:

<img width="100" src="/img/question/wenti-yuming-01.png"  width="550" height="550" alt="AikanPro"/> 

点击反向代理:

<img width="100" src="/img/question/wenti-yuming-02.png"  width="550" height="550" alt="AikanPro"/> 

设置反向代理:

<img width="100" src="/img/question/wenti-yuming-03.png"  width="550" height="550" alt="AikanPro"/> 